GROQ_API_KEY=
JWT_SECRET=change-me-in-production-please
JWT_ALGORITHM=HS256
JWT_EXPIRE_MIN=60
DATABASE_URL=sqlite:///./medscribe.db
CORS_ORIGINS=http://localhost:5173
# Google OAuth client ID (Web application). Get one at
# https://console.cloud.google.com/apis/credentials. Optional — leave empty
# to disable Google sign-in and use only email/password.
GOOGLE_OAUTH_CLIENT_ID=
